/** 
 * Aucun traitement particulier.
 * 
 * Trie les variables non assignées selon leur ordre initial. 
 * e.g : 0:0 < 0:1 < 0:2 < ... < 8:8 
 */
  function basic(assignement, unassigned) {
    return sortByName(unassigned)
  }